import { BootstrapSettingsInterface } from '../app/interfaces/bootstrap.settings.interface';

const settings: BootstrapSettingsInterface = {
  launchBoosterEventName: 'BOOSTER_LAUNCH',
  startRenderEventName: 'BOOSTER_RENDER_START'
};

export default settings;
